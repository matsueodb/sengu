<%= form_for(@section, html: {class: "form-horizontal well"}) do |f| %>
  <%= render partial: "form", locals: {f: f} %>
  <%= button_tag_with_icon t("shared.new"), class: "btn btn-primary", icon: "edit" %>
  <%= link_to_back sections_path, icon: "edit" %>
<% end %>
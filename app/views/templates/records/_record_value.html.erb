<%= render partial: "templates/records/record_value/#{element.input_type.name}",
  locals: {
    element: element,
    record: record,
    template: template,
    values: values,
    item_number: item_number
  } %>
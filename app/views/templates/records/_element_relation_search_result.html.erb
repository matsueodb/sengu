<%
  case
  when @element_relation_content_search.input_type.template?
%>
  <%= render partial: 'element_relation_search_result_template', locals: {element: element, values: values, source_element: source_element} %>
<% when @element_relation_content_search.input_type.vocabulary? %>
  <%= render partial: 'element_relation_search_result_vocabulary', locals: {element: element, values: values} %>
<% end %>

<%= paginate values, remote: true, method: :post, window: Settings.paginate.modal.window %>




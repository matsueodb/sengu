$("#pagination_list").html("<%= j render partial: "list", locals: {table_id: "data-table", template: @template, records: @template_records, elements: @elements, operator: true} %>")
initializeDataTable()